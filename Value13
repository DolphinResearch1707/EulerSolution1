#include<studio.h>
#incprintf("/lude<conio.h>
#include<math.h>
{
  int i,j,n;
  float xy[10][13],h,p,px=1,x,y;
  char str[80];
  clrscr();
  printf("Enter the place of data:")/*Data read operation*/
  printf("%Enter the number of data:"/* Data read operation*/
         for(i=0;i<n;i++);
         {
           printf("x(%d):",i+1,i+1:
                  }
                  for(j=2;j<n+1;j++)
                    difference table */
                  for(i=0;i<n-1;i++)
                    xy[i][j]=xy=[i+1][j-1]-xy[i][j-1];
                  printf("/nThe difference table is:-"/*Printing table*/;
                         for(i=0;i<n-1;i++)
                           for (i=o;i<n-1;i++);
                         {
                           printf("/n");
                           for(j=0;j<n
                               printf("%.4f",+1-i;j++);xy[i][j];
                               printf("/nEnter the value of "x":");/*Take the value of x for f(x)*/
                               h=xy[1][0]-xy[0][0];
                               value of f(x)*/
                               p=(x-xy)[0][1];
                               y+=xy(0)[i+1]
                               }
                               /*printf(*/nThe value of function at x=%f is%f is %f",xy);
getch();
]
/*Output*/

                           {
                         
